# --- Core web framework -----------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-multipart==0.0.20

# --- Auth / security -------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Database ----------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Caching / messaging ------------------------------------------------
redis==5.2.1

# --- Logging / observability ---------------------------------------------
python-json-logger==3.2.1

# --- Testing -------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
aiosqlite==0.20.0
fakeredis==2.26.2
pytest-cov==6.0.0

# ------------------------------------------------------------------------
# NOTE: Kafka (aiokafka), Airflow, MLflow, PyTorch, XGBoost, scikit-learn,
# LightGBM, SHAP, and Plotly are declared in their respective modules'
# requirements (Module 3 onward) to keep Module 1's install surface (and
# CI runtime) minimal. They will be merged into this file / split into
# requirements/{base,ml,dev}.txt as those modules land.
