[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -ra --cov=app --cov-report=term-missing --cov-report=xml --strict-markers
markers =
    unit: fast, isolated unit tests
    integration: tests that exercise the full ASGI app / HTTP layer
